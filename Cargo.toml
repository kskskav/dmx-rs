[package]
name = "dm_x"
description = "A library for using dmenu as an option picker"
version = "0.1.0"
edition = "2021"


[dependencies]

[dev-dependencies]
libc = "^0.2"
once_cell = "^1.12"
serde = { version = "^1.0", features=["derive"] }
serde_json = "^1.0"